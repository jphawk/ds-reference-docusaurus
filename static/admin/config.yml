backend:
  name: git-gateway
  branch: master
  commit_messages:
    create: Create {{collection}} {{fields.name}}
    update: Update {{collection}} {{fields.name}}
    delete: Delete {{collection}} {{fields.name}}
    uploadMedia: Upload to {{media_folder}}
    deleteMedia: Delete from {{media_folder}}

media_folder: static/img
public_folder: static/img

collections:
  - name: 'docs'
    label: 'Docs'
    label_singular: 'Doc'
    folder: 'docs'
    extension: md
    widget: list
    create: true
    fields:
      - {label: 'Name', name: 'name'}
      - {label: 'Menu', name: 'menu', required: false}
      - {label: 'Body', name: 'body', widget: 'markdown'}
